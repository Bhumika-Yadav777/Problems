class Solution {
public:
    int hammingWeight(int n) {
        int count=0;
        std::string binary = std::bitset<32>(n).to_string();
        for(int i=0;i<binary.size();i++){
            if(binary[i]=='1'){
                count++;
            }
        }
        return count;
    }
};
